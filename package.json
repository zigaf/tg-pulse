{
  "name": "tgpulse",
  "private": true,
  "version": "0.1.0",
  "description": "Telegram channel traffic attribution & analytics platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:bot": "npm run dev -w apps/bot",
    "build:web": "npm run build -w packages/db && npm run build -w apps/web",
    "build:bot": "npm run build -w packages/db && npm run build -w apps/bot",
    "db:generate": "npm run generate -w packages/db",
    "db:migrate": "npm run migrate:dev -w packages/db",
    "typecheck": "npm run typecheck -w apps/web -w apps/bot -w packages/db"
  }
}
